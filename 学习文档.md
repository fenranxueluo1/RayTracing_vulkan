# RayTracing Vulkan 学习文档

> 面向新手的项目学习指南：本工程是 TheCherno 的《RayTracing》教程系列的基础框架，
> 由原版的 GLFW + 老式 Vulkan 移植为 **SDL3 + Vulkan 1.4**，
> 并使用 CMake 构建，支持 Windows / Linux 双平台。

---

## 1. 这个项目是什么？

这是一个"搭好的房子"，用来跟随 TheCherno 的 [RayTracing 视频系列](https://www.youtube.com/watch?v=gfW1Fhd9u9Q) 逐集实现一个光追渲染器。

它目前做的事情很简单：**打开一个窗口，用 ImGui 画一个可停靠（Docking）的编辑器界面**。
后续每一集你都会在这个基础上往里面加相机、球体、光追着色器等。

一句话概括技术栈：

| 组件 | 用途 | 版本 |
| ---- | ---- | ---- |
| **SDL3** | 创建窗口、处理鼠标键盘事件、创建 Vulkan 表面 | 3.4.14 |
| **Vulkan** | GPU 渲染 API，负责把画面画到窗口 | 1.4（SDK 1.4.357） |
| **volk** | Vulkan 函数加载器（动态加载 `vk*` 函数指针） | 随 SDK 附带 |
| **Dear ImGui** | 即时模式 GUI，用于编辑器界面 | docking 分支 |
| **glm** | 数学库（向量、矩阵），后续做相机/光线会用 | 纯头文件 |
| **SDL3_image** | 图片解码（替代原版 stb_image） | 3.4.4 |
| **Noto Sans SC** | 界面默认字体（含中文，SIL OFL 协议可免费商用，已随程序分发） | - |
| **CMake** | 跨平台构建系统 | ≥ 4.3 |

---

## 2. 环境准备

### 2.1 必须安装的东西

**Vulkan SDK（必须）**
- Windows：去 [LunarG](https://vulkan.lunarg.com/) 下载安装，安装器会自动设置环境变量 `VULKAN_SDK`。
- Linux：下载 SDK 后需要先执行 `source <VulkanSDK>/setup-env.sh`，让 `VULKAN_SDK` 指向 SDK 根目录。

**编译器**
- Windows：推荐 MinGW-w64（本工程预设即 MinGW），或 Visual Studio（MSVC）。
- Linux：GCC / Clang。

**CMake**（版本 ≥ 4.3）

### 2.2 第三方依赖

依赖集中在 `D:/library`（Windows 上）。项目通过 CMake 缓存变量 `RAYTRACING_DEP_ROOT` 指向它，
默认值为 `D:/library`。其中：
- `SDL3-3.4.14-mingw` / `SDL3-3.4.14-vc`：SDL3 预编译库（按编译器自动选择）。
- `SDL3_image-3.4.4-mingw`：SDL3_image 预编译库。
- `imgui-docking`：Dear ImGui（docking 分支），已把需要的核心文件复制进本工程的 `Dependencies/imgui`。
- `glm`：数学库，已复制进 `Dependencies/glm`。

> Linux 下 SDL3 / SDL3_image 由系统包管理器提供（`libsdl3-dev` 等），CMake 会自动走 pkg-config。

---

## 3. 目录结构

```
RayTracing_vulkan/
├── CMakeLists.txt            # 顶层构建脚本：找 Vulkan、定义依赖路径、引入子项目
├── CMakePresets.json         # 预设：windows-debug / windows-release / linux-debug / ...
├── assets/
│   └── fonts/
│       ├── NotoSansSC-VF.ttf # 默认字体（含中文，OFL 协议，构建时自动拷贝到 exe 旁）
│       └── OFL.txt           # SIL Open Font License（字体授权文件）
├── Dependencies/
│   ├── glm/                  # 数学库（纯头文件，来自 D:/library/glm）
│   └── imgui/                # Dear ImGui docking 版（来自 D:/library/imgui-docking）
├── Walnut/                   # 框架静态库（TheCherno 的 Walnut 模板移植版）
│   ├── CMakeLists.txt
│   └── src/Walnut/
│       ├── Application.h/.cpp  # ★ 核心：窗口 + Vulkan 上下文 + 主循环 + ImGui
│       ├── Vulkan.h            # ★ 统一包含 vulkan.h 与 volk.h
│       ├── EntryPoint.h        # 程序入口（跨平台 main）
│       ├── Image.h/.cpp        # 图像类：加载图片并上传为 GPU 纹理（用 SDL3_image）
│       ├── Layer.h             # 图层基类（你的业务代码写在这里）
│       ├── Random.h/.cpp       # 随机数工具（光追会用到）
│       ├── Timer.h             # 计时工具
│       ├── Input/              # 键盘/鼠标输入封装（SDL3 版）
│       └── ImGui/              # ImGui 后端编译
└── RayTracing/                # 你的应用程序（可执行文件）
    ├── CMakeLists.txt
    └── src/WalnutApp.cpp      # ★ 你要改的主文件：在这里写你的图层
```

---

## 4. 构建与运行

### Windows（MinGW）

```bash
cd D:/Vulkan_Project/RayTracing_vulkan
cmake --preset windows-debug      # 配置（生成 build/ 目录）
cmake --build  --preset windows-debug   # 编译
```

运行：`build/bin/RayTracing.exe`（SDL3.dll / SDL3_image.dll / MinGW 运行库已自动拷贝到 exe 旁边）。

> 一键「编译并运行」：`cmake --build --preset windows-debug-run`

### Linux

```bash
source /path/to/VulkanSDK/setup-env.sh   # 设置 VULKAN_SDK
cd RayTracing_vulkan
cmake --preset linux-debug
cmake --build --preset linux-debug
```

运行：`./build/bin/RayTracing`

### 打开 IDE

- Visual Studio：`cmake --preset windows-debug -G "Visual Studio 17 2022"` 后直接打开生成的 `.sln`。
- VS Code / CLion / Zed：直接以本目录为根目录打开，使用 `CMakePresets.json` 即可。

---

## 5. 依赖是怎么被引用的（CMake 讲解）

顶层 `CMakeLists.txt` 做了几件关键的事，顺序如下：

1. **设置 C++ 标准为 C++23**
   ```cmake
   set(CMAKE_CXX_STANDARD 23)
   ```
   为什么必须 ≥ C++20？因为代码里大量使用 Vulkan 结构体的**指定初始化器**写法，例如：
   ```cpp
   VkImageCreateInfo info{ .sType = VK_STRUCTURE_TYPE_IMAGE_CREATE_INFO, ... };
   ```
   这是 C++20 才有的语法，也是 Nexus 项目的统一写法。

2. **查找 Vulkan SDK**
   ```cmake
   find_package(Vulkan REQUIRED COMPONENTS volk)
   ```
   - `Vulkan::Vulkan`：链接 `vulkan-1.lib`（Win）或 `libvulkan.so`（Linux）。
   - `volk`：提供 `volk.h` 头文件搜索路径。
   - 同时用 `find_path(RAYTRACING_VOLK_INCLUDE_DIR ...)` 归一化 volk 头文件的实际目录，
     因为不同来源（SDK / 发行版包）的 `volk.h` 所在目录不同。

3. **Windows 上定位第三方预编译库**
   ```cmake
   set(RAYTRACING_DEP_ROOT "D:/library" CACHE PATH "...")
   if(MINGW)
     set(RAYTRACING_SDL3_DIR ".../SDL3-3.4.14-mingw/.../cmake/SDL3")
   elseif(MSVC)
     set(RAYTRACING_SDL3_DIR ".../SDL3-3.4.14-vc/cmake")
   endif()
   ```
   按编译器（MinGW 还是 MSVC）自动选择对应的 SDL3 / SDL3_image 包。

4. **引入两个子项目**
   ```cmake
   add_subdirectory(Walnut)
   add_subdirectory(RayTracing)
   ```

在 `Walnut/CMakeLists.txt` 里：
- `Walnut` 是静态库，负责编译框架代码 + ImGui。
- 通过 `target_link_libraries(Walnut PUBLIC SDL3::SDL3)` 把 SDL3 的头文件路径**传递**给使用者（RayTracing）。
- `SDL3_image` 只被 `Image.cpp` 使用，所以是 `PRIVATE`。
- 编译宏 `IMGUI_IMPL_VULKAN_USE_VOLK` 告诉 ImGui 的 Vulkan 后端改用 volk 拿函数指针。

---

## 6. Vulkan 基础概念（写给新手）

Vulkan 是一套"贴近硬件"的图形 API。它要求你把很多细节都自己管起来。
以下是理解本项目所需的最小概念集：

### 6.1 对象层级（从大到小）

```
VkInstance（整个程序的 Vulkan 实例）
 └─ VkPhysicalDevice（物理 GPU，如你的独显）
     └─ VkDevice（逻辑设备 = 你和 GPU 之间的一扇门）
         └─ VkQueue（命令队列：你把命令排进队列，GPU 执行）
             └─ VkCommandBuffer（命令缓冲：装着一串 GPU 命令）
```

- **实例**：程序开始时要创建一次，声明"我要用 Vulkan、需要哪些扩展和校验层"。
- **物理设备**：枚举电脑上的 GPU，挑一个合适的（本工程优先选独立显卡）。
- **逻辑设备**：基于物理设备创建，指定要启用哪些**特性**（比如本项目的动态渲染、同步2）。
- **队列**：图形命令要提交到队列上执行。本项目只用一个支持"图形 + 呈现"的队列。

### 6.2 交换链（Swapchain）

窗口的本质是一堆连续播放的图片。交换链就是"图片池"：

```
   [图片0] [图片1] [图片2] ...
     ↑获取(acquire)    ↑画完展示(present)
```

每帧流程：
1. `vkAcquireNextImageKHR`：从交换链取一张图片。
2. 往命令缓冲里记录"把画面画到这张图片上"的命令。
3. `vkQueueSubmit`：提交命令。
4. `vkQueuePresentKHR`：把这张图片交给窗口显示。

窗口大小变了，交换链要重建（项目里用 `g_SwapChainRebuild` 标记）。

### 6.3 渲染管线（Pipeline）

告诉 GPU"怎么把三角形变成像素"的一系列配置：顶点输入、光栅化、混合、着色器……
本项目用的 ImGui 管线由后端自动创建，你要理解的核心是**动态渲染（Dynamic Rendering）**：

```cpp
vkCmdBeginRendering(cmd, &renderingInfo);   // 不再需要创建 VkRenderPass / VkFramebuffer
  ...画东西...
vkCmdEndRendering(cmd);
```
这是 Vulkan 1.3 起推荐的现代写法，本工程用的是 Vulkan 1.4。

### 6.4 同步（Synchronization）

GPU 异步执行，所以需要同步原语保证"上一帧画完了才能复用这张图"：
- **Fence（围栏）**：CPU 等 GPU（比如等命令执行完）。
- **Semaphore（信号量）**：GPU 内部各步骤之间互相等（比如等图片获取完成再开始画）。

本项目使用 Vulkan 1.3 的**同步2**（Synchronization2）：`vkCmdPipelineBarrier2`、`vkQueueSubmit2`。

### 6.5 volk 是干什么的？

默认情况下 Vulkan 代码链接 `vulkan-1.lib`，所有 `vk*` 函数符号在编译期就确定。
而 **volk** 改为在运行时动态加载函数指针，好处是：
- 支持加载新驱动才有的扩展函数；
- 一个二进制能兼容更多驱动；
- 是 Nexus 项目采用的方式。

用法要点：
```cpp
// Walnut/src/Walnut/Vulkan.h
#define VK_NO_PROTOTYPES          // 不要 vulkan.h 的静态函数声明
#include <vulkan/vulkan.h>
#include <volk.h>                 // 声明全部 vk* 函数

// Walnut/src/Walnut/Application.cpp 顶部
#define VOLK_IMPLEMENTATION       // 只在这里生成 volk 的实现（函数指针表）
#include "Application.h"
```

---

## 7. 代码走读：程序是怎么启动的？

入口在 `RayTracing/src/WalnutApp.cpp`：

```cpp
// 1. 声明一个"图层"：将来你的光追 UI 就写在 OnUIRender() 里
class ExampleLayer : public Walnut::Layer {
    void OnUIRender() override { /* 画 ImGui 窗口 */ }
};

// 2. 框架要求你实现这个函数，返回你的应用对象
Walnut::Application* Walnut::CreateApplication(int argc, char** argv)
{
    Walnut::ApplicationSpecification spec;
    spec.Name = "Walnut Example";        // 窗口标题
    Walnut::Application* app = new Walnut::Application(spec);
    app->PushLayer<ExampleLayer>();      // 把你的图层塞进应用
    return app;
}
```

真正的 `main()` 在 `Walnut/src/Walnut/EntryPoint.h` 里，它调 `CreateApplication` 并进入主循环。

---

## 8. 一帧是怎么画出来的？（Application.cpp 主流程）

所有核心逻辑都在 `Walnut/src/Walnut/Application.cpp`。分两个阶段看。

### 8.1 初始化（构造 `Application` → `Init()`，Application.cpp:551）

按顺序做这几件事：

| 步骤 | 位置 | 说明 |
| ---- | ---- | ---- |
| 初始化 SDL3 | `Init()` | `SDL_Init(SDL_INIT_VIDEO)` |
| 创建窗口 | `Init()` | `SDL_CreateWindow(..., SDL_WINDOW_VULKAN \| RESIZABLE)` |
| 收集 SDL 要求的实例扩展 | `Init()` | `SDL_Vulkan_GetInstanceExtensions` |
| **创建 Vulkan 上下文** | `SetupVulkan()` (Application.cpp:86) | 见下表 |
| **创建交换链** | `SetupVulkanWindow()` (Application.cpp:274) | 选表面格式、呈现模式，创建交换链 |
| 初始化 ImGui | `Init()` | 创建上下文、SDL3 + Vulkan 后端、加载随程序分发的 Noto Sans SC 作为默认字体 |
| 初始化输入 | `Init()` | `ImGui_ImplSDL3_InitForVulkan` |

`SetupVulkan()` 内部依次完成：
1. `volkInitialize()` —— 启动 volk。
2. `vkCreateInstance` —— 创建实例，Debug 下附带校验层 `VK_LAYER_KHRONOS_validation` 和 `VK_EXT_debug_utils`。
3. `SDL_Vulkan_CreateSurface` —— 创建窗口表面（**必须在选队列族之前**，因为选队列要看"能否呈现到该表面"）。
4. 枚举物理设备，**优先选独显**。
5. 找"同时支持图形 + 呈现"的队列族。
6. `vkCreateDevice` —— 创建逻辑设备，用特性链启用 `dynamicRendering` 和 `synchronization2`（Vulkan 1.4 写法，见 Application.cpp:226）。
7. `vkCreateDescriptorPool` —— 给 ImGui 用的描述符池。

> **新手常见疑问**：为什么要先建表面再选队列？因为 `vkGetPhysicalDeviceSurfaceSupportKHR` 需要表面句柄来判断某个队列族能否呈现。当初版代码顺序搞反，导致 `surface is VK_NULL_HANDLE` 崩溃（这是个很好的学习案例）。

### 8.2 主循环（`Run()`，Application.cpp:673）

每一帧大概做这几步：

```cpp
while (m_Running)
{
    // 1. 处理事件（键盘/鼠标/窗口缩放/关闭）→ SDL_PollEvent
    // 2. 若需要则重建交换链
    // 3. 让图层执行每帧逻辑  → layer->OnUpdate(ts)
    // 4. 开始一帧 ImGui       → ImGui::NewFrame()
    // 5. 渲染你的 UI          → layer->OnUIRender()（在 DockSpace 里）
    // 6. 记录并提交命令       → FrameRender()   (Application.cpp:321)
    // 7. 展示                 → FramePresent()  (Application.cpp:503)
}
```

`FrameRender()` 里对每帧图片做：
- `vkAcquireNextImageKHR` 获取图片；
- 用同步2 的 `vkCmdPipelineBarrier2` 把图片从"呈现布局"切换到"颜色附件布局"；
- `vkCmdBeginRendering` 开始动态渲染；
- `ImGui_ImplVulkan_RenderDrawData` 记录 ImGui 的绘制命令；
- `vkCmdEndRendering` 结束渲染，再把图片转回呈现布局；
- `vkQueueSubmit2` 提交（等"图片已获取"信号量 → 画 → 发出"画完了"信号量）。

---

## 9. Walnut 提供给你的工具类

### 9.1 Layer（图层）— 你的主要战场
`Layer.h` 里是一个抽象基类，你继承它并覆写：
```cpp
virtual void OnUpdate(float ts) {}   // 每帧逻辑（移动相机、更新场景…）
virtual void OnUIRender() {}         // 画 ImGui 界面
virtual void OnAttach()  {}          // 图层挂载时（加载资源…）
virtual void OnDetach()  {}          // 图层卸载时（清理…）
```

### 9.2 Image（图像）
`Walnut::Image` 用 **SDL3_image** 解码图片并上传为 GPU 纹理，返回给 ImGui 使用的 `VkDescriptorSet`：
```cpp
auto image = std::make_shared<Walnut::Image>("textures/sky.hdr");
ImGui::Image(image->GetDescriptorSet(), { image->GetWidth(), image->GetHeight() });
```
- 普通图片 → 统一转成 `SDL_PIXELFORMAT_RGBA32`（内存中 R,G,B,A），对应 `VK_FORMAT_R8G8B8A8_UNORM`。
- HDR 图片（`.hdr`）→ 保留为浮点格式，对应 `VK_FORMAT_R32G32B32A32_SFLOAT`（后续做 HDR 天空盒会用）。

### 9.3 Input（输入）
```cpp
Walnut::Input::IsKeyDown(Walnut::KeyCode::W);          // 键盘
Walnut::Input::IsMouseButtonDown(Walnut::MouseButton::Left);
Walnut::Input::GetMousePosition();                     // 鼠标坐标
Walnut::Input::SetCursorMode(Walnut::CursorMode::Locked); // 捕获鼠标（第一人称相机）
```
键码 `KeyCodes.h` 已改为 SDL3 的扫描码值。

---

## 10. 常见问题（FAQ）

**Q1：启动时出现 `image has not been acquired from VkSwapchainKHR` 校验警告？**
这是 ImGui docking 版后端（1.92.9）的一个已知无害 bug（[ocornut/imgui#9496](https://github.com/ocornut/imgui/issues/9496)），
仅在启动/缩放窗口时出现，不影响运行；Release（无校验层）不显示。

**Q2：Debug 与 Release 能同时保留吗？**
预设共用同一个 `build/` 目录。切预设等于重新配置构建类型，重新编译即可。

**Q3：想换显卡怎么办？**
`SetupVulkan()` 里"优先选独显"的逻辑：遍历物理设备，遇到 `VK_PHYSICAL_DEVICE_TYPE_DISCRETE_GPU` 就选它，
否则退回第一个。

**Q4：为什么一定要 `VULKAN_SDK` 环境变量？**
`find_package(Vulkan)` 依赖它定位 SDK（`FindVulkan` 查找 `$VULKAN_SDK/include` 与 `$VULKAN_SDK/lib`）。
Linux 记得先 `source setup-env.sh`。

**Q5：添加自己的着色器怎么做？**
本项目目前没有着色器（ImGui 的由后端内置）。当你开始写光追计算着色器时，
可参考 Nexus 的做法：用 glslc 在构建期把 `.comp` 编译成 `.spv`，运行时加载。
届时把顶层 `find_package(Vulkan ...)` 改为 `COMPONENTS volk glslc` 即可。

**Q6：中文显示成方块/乱码（豆腐块）？**
本项目已经把 **Noto Sans SC**（SIL OFL 协议，可免费商用）作为默认字体**随程序分发**，
不依赖系统字体：
- 字体源文件：`assets/fonts/NotoSansSC-VF.ttf`（构建时由 CMake 自动拷贝到 exe 同目录 `fonts/`）；
- 运行时：`Application::Init()` 用 `SDL_GetBasePath()` 定位 exe 目录，再加载 `fonts/NotoSansSC-VF.ttf`
  并设置为 ImGui 默认字体（`GetGlyphRangesChineseFull()` 覆盖全部中文字符）；
- 发布程序时，把 exe 与 `fonts/` 文件夹一起分发即可。

只要字体文件在 exe 旁，任何系统（Windows / Linux）都能正常显示中文。
请同时确保你的 `.cpp` 源文件以 **UTF-8** 编码保存（否则字符串字面量本身就会损坏）。

**Q7：MSVC 构建报找不到 SDL3_image？**
`D:/library` 目前只有 MinGW 版的 SDL3_image。用 MSVC 构建请先下载 `SDL3_image-3.4.4-vc` 放入 `D:/library`。

---

## 11. 下一步学习建议

1. 先跑起来，改改 `WalnutApp.cpp` 里的窗口文字和颜色。
2. 跟随 TheCherno 系列第 2~4 集，在 `OnUpdate()` 里用 `Walnut::Input` 做一个可移动相机。
3. 用 `Walnut::Image` 加载一张 HDR 环境贴图试试 `ImGui::Image` 显示。
4. 用 `Walnut::Random` 生成球体数据，把场景画进一张 `Walnut::Image`（RGBA32F）再显示——这就是光追的第一步。
5. 当画面卡顿时，打开 GPU 调试（校验层已内置在 Debug 构建），学会读 validation 报错。

祝学习愉快！